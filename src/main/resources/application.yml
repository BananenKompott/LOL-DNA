riot:
  api:
    # Loaded from an external, git-ignored file or environment variable.
    # Example (application-local.yml, not committed):
    # riot:
    #   api:
    #     key: RGAPI-xxxx
    key: ${RIOT_API_KEY:}